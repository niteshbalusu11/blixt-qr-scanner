import { requireNativeComponent } from 'react-native';

// Exporting the CustomQRCodeScanner component
const CustomQRCodeScanner = requireNativeComponent('CustomQRCodeScanner');

export default CustomQRCodeScanner;
